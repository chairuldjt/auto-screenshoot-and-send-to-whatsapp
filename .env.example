# Example environment variables for Auto Screenshot WhatsApp Bot
# Copy this file to `.env` and fill values. Do NOT commit your real `.env`.

# WhatsApp group ID where screenshots will be sent (e.g. 123456789@g.us)
GROUP_ID=

# Cron schedule for screenshots (default: every hour)
# Example for every hour: 0 * * * *
CRON_SCHEDULE=0 * * * *
